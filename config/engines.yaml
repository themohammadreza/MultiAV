engines:
  clamav:
    enabled: true
    weight: 0.35
    timeout: 30
  yara:
    enabled: true
    weight: 0.30
    timeout: 60
  avast:
    enabled: true
    weight: 0.30  # HTTP scanner; tune higher if using fresh definitions
    timeout: 120  # allow extra time for larger samples/engine warm-up
  windows-defender:
    enabled: true
    weight: 0.35
    timeout: 120
